# Force HTTP → HTTPS (keep same host)
http://*   https://:splat   301!

# Canonical: www → apex
https://www.reedssolutionsllc.org/*   https://reedssolutionsllc.org/:splat   301!

# Serve 404 for missing routes (NO SPA fallback)
 /*   /404.html   404
